[tool.ruff]
target-version = "py313"
line-length = 120
src = ["src", "tests"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

[tool.coverage.run]
source = ["freemail_api"]

[tool.coverage.report]
fail_under = 80
show_missing = true
skip_covered = false
